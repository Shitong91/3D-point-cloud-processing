从vlp16手册中看，

lidar的坐标系是 y forward
                x right
                z up

但程序中给的是  x forward
                y left
                z up
也就是说程序中的坐标系和ros坐标系是相同的，
应该是因为程序使用的是ROS平台，应该是在使用ros
采集的时候已经把坐标系同步到ros下了

所以以后直接默认下都使用第二个坐标系统。
